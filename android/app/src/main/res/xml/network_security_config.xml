<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for TOMAT
  - Allows HTTPS to the production sslip.io server (Let's Encrypt cert — trusted by default)
  - Allows cleartext HTTP to localhost (dev builds / Capacitor live-reload)
  - Allows cleartext HTTP to 10.x / 192.168.x RFC-1918 ranges (local dev server)
-->
<network-security-config>
    <!-- Production server — HTTPS only, system trust anchors are sufficient -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">sslip.io</domain>
    </domain-config>

    <!-- Local development: localhost + RFC-1918 ranges may use HTTP -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>

    <!-- Base config: all other domains follow system defaults (HTTPS required) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
