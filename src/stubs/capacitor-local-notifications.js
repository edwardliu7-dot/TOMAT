// Stub for @capacitor/local-notifications in browser/dev mode.
// In the APK, Capacitor injects the real plugin via the native bridge;
// the stub is never reached because getPlugin() bails early on !window.Capacitor.
export const LocalNotifications = null
