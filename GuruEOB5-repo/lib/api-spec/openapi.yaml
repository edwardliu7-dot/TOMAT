openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification for GuruEOB5 - school management app for teachers
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Authentication
  - name: teachers
    description: Teacher accounts
  - name: students
    description: Student records
  - name: subjects
    description: Subjects taught
  - name: journal
    description: Teaching journal entries
  - name: attendance
    description: Student attendance
  - name: grades
    description: Student grades
  - name: points
    description: Student points/behavior
  - name: documents
    description: Administrative documents
  - name: tujuan-pembelajaran
    description: Tujuan Pembelajaran (learning objectives), manual entry or AI-assisted import from any file format
  - name: dashboard
    description: Dashboard aggregates
  - name: academic-calendar
    description: Academic calendar and weeks (admin-managed)
  - name: prosem
    description: Program semester (per-teacher lesson plan)
  - name: info-pekanan
    description: Weekly info (rencana vs realisasi)
  - name: modul-ajar
    description: AI-generated Kurikulum Merdeka teaching modules
  - name: soal-otomatis
    description: AI-generated practice questions/quizzes
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/register:
    post:
      operationId: register
      tags: [auth]
      summary: Register a new teacher account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterInput"
      responses:
        "200":
          description: Registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Teacher"

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Log in with username and password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Logged in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Teacher"

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Log out current session
      responses:
        "200":
          description: Logged out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get the current logged-in teacher
      responses:
        "200":
          description: Current teacher
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Teacher"

  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      summary: Get dashboard summary stats
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /kepsek/overview:
    get:
      operationId: getKepsekOverview
      tags: [dashboard]
      summary: Kepala sekolah progress overview across all teachers
      responses:
        "200":
          description: Overview of teacher work progress
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KepsekOverview"

  /kurikulum/overview:
    get:
      operationId: getKurikulumOverview
      tags: [dashboard]
      summary: Wakasek kurikulum view of all teachers' administrative documents
      responses:
        "200":
          description: Documents per teacher and subject
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KurikulumOverview"

  /kesiswaan/overview:
    get:
      operationId: getKesiswaanOverview
      tags: [dashboard]
      summary: Wakasek kesiswaan recap of attendance and points per class
      responses:
        "200":
          description: Attendance and points recap
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KesiswaanOverview"

  /kesiswaan/absensi-siswa:
    get:
      operationId: getKesiswaanAbsensiSiswa
      tags: [dashboard]
      summary: Per-student attendance breakdown for wakasek kesiswaan
      parameters:
        - name: calendarId
          in: query
          required: false
          schema:
            type: string
          description: Filter attendance to the date range of this academic calendar semester.
      responses:
        "200":
          description: Per-student attendance data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KesiswaanSiswaAbsensi"

  /kepsek/jurnal:
    get:
      operationId: getKepsekJurnal
      tags: [dashboard]
      summary: Kepala sekolah view of all journal entries in the school
      responses:
        "200":
          description: Journal entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleJurnalResponse"

  /kurikulum/jurnal:
    get:
      operationId: getKurikulumJurnal
      tags: [dashboard]
      summary: Kurikulum view of all teacher journal entries
      responses:
        "200":
          description: Journal entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleJurnalResponse"

  /walikelas/jurnal:
    get:
      operationId: getWaliKelasJurnal
      tags: [dashboard]
      summary: Wali kelas view of journal entries for their homeroom class
      responses:
        "200":
          description: Journal entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleJurnalResponse"

  /walikelas/rekap:
    get:
      operationId: getWaliKelasRekap
      tags: [dashboard]
      summary: Wali kelas recap for the current teacher's homeroom class
      responses:
        "200":
          description: Homeroom class recap
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WaliKelasRekap"

  /walikelas/akun-siswa:
    get:
      operationId: listStudentAccounts
      tags: [dashboard]
      summary: List every student in the current wali kelas's class with their account status
      responses:
        "200":
          description: List of students with account status
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    studentId:
                      type: string
                    namaLengkap:
                      type: string
                    hasAccount:
                      type: boolean
                    username:
                      type: string
                      nullable: true
                    password:
                      type: string
                      nullable: true
                  required: [studentId, namaLengkap, hasAccount, username, password]

  /walikelas/akun-siswa/{id}/generate:
    post:
      operationId: generateStudentAccount
      tags: [dashboard]
      summary: Generate (or fetch existing) a TOMAT/BLP account for a student in the current wali kelas's class
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateStudentAccountInput"
      responses:
        "201":
          description: Generated account credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentAccount"

  /walikelas/akun-siswa/generate-all:
    post:
      operationId: generateAllStudentAccounts
      tags: [dashboard]
      summary: Generate accounts for every student in the current wali kelas's class who doesn't have one yet
      responses:
        "200":
          description: Bulk generation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  generated:
                    type: integer
                  alreadyExisted:
                    type: integer
                  accounts:
                    type: array
                    items:
                      $ref: "#/components/schemas/StudentAccount"
                required: [generated, alreadyExisted, accounts]

  /walikelas/akun-siswa/cards:
    get:
      operationId: downloadAllStudentAccountCards
      tags: [dashboard]
      summary: Download a single PDF containing a printable account card for every generated account in the class
      responses:
        "200":
          description: PDF file
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  /walikelas/akun-siswa/{id}:
    get:
      operationId: getStudentAccount
      tags: [dashboard]
      summary: Get an already-generated account's credentials for a student
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Account credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentAccount"

  /walikelas/akun-siswa/{id}/card:
    get:
      operationId: downloadStudentAccountCard
      tags: [dashboard]
      summary: Download a single printable account card as PDF
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: PDF file
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  /teachers:
    get:
      operationId: listTeachers
      tags: [teachers]
      summary: List all teachers
      responses:
        "200":
          description: List of teachers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Teacher"
  /teachers/progress:
    get:
      operationId: listTeachersProgress
      tags: [teachers]
      summary: Per-teacher progress stats (jurnal bulan ini, dokumen, kelengkapan) — school admin only
      responses:
        "200":
          description: Progress stats per teacher
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TeacherProgress"

  /teachers/{id}:
    get:
      operationId: getTeacher
      tags: [teachers]
      summary: Get a teacher by id
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Teacher
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Teacher"
    patch:
      operationId: updateTeacher
      tags: [teachers]
      summary: Update a teacher
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTeacherInput"
      responses:
        "200":
          description: Updated teacher
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Teacher"
    delete:
      operationId: deleteTeacher
      tags: [teachers]
      summary: Delete a teacher
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /students:
    get:
      operationId: listStudents
      tags: [students]
      summary: List students
      parameters:
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of students
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Student"
    post:
      operationId: createStudent
      tags: [students]
      summary: Create a student
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentInput"
      responses:
        "200":
          description: Created student
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Student"
  /students/import/analyze:
    post:
      operationId: analyzeStudentImport
      tags: [students]
      summary: Analyze raw spreadsheet rows with AI and map them to student fields
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentImportAnalyzeInput"
      responses:
        "200":
          description: AI-mapped student rows for verification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentImportAnalyzeResult"
  /students/bulk:
    post:
      operationId: bulkCreateStudents
      tags: [students]
      summary: Create many students at once
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkCreateStudentsInput"
      responses:
        "200":
          description: Number of students created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkCreateStudentsResult"
  /students/{id}:
    get:
      operationId: getStudent
      tags: [students]
      summary: Get a student by id
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Student
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Student"
    patch:
      operationId: updateStudent
      tags: [students]
      summary: Update a student
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentInput"
      responses:
        "200":
          description: Updated student
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Student"
    delete:
      operationId: deleteStudent
      tags: [students]
      summary: Delete a student
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /subjects:
    get:
      operationId: listSubjects
      tags: [subjects]
      summary: List subjects
      responses:
        "200":
          description: List of subjects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Subject"
    post:
      operationId: createSubject
      tags: [subjects]
      summary: Create a subject
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubjectInput"
      responses:
        "200":
          description: Created subject
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subject"
  /subjects/{id}:
    patch:
      operationId: updateSubject
      tags: [subjects]
      summary: Update a subject
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubjectInput"
      responses:
        "200":
          description: Updated subject
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subject"
    delete:
      operationId: deleteSubject
      tags: [subjects]
      summary: Delete a subject
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /documents:
    get:
      operationId: listDocuments
      tags: [documents]
      summary: List administrative documents
      parameters:
        - name: subjectId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of documents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminDocument"
    post:
      operationId: createDocument
      tags: [documents]
      summary: Create an administrative document
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminDocumentInput"
      responses:
        "200":
          description: Created document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminDocument"
  /documents/{id}:
    delete:
      operationId: deleteDocument
      tags: [documents]
      summary: Delete a document
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /documents/{id}/file:
    get:
      operationId: getDocumentFile
      tags: [documents]
      summary: Download/view a document's file content
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Raw file content, streamed with the document's original content type
        "404":
          description: Document not found

  /tp:
    get:
      operationId: listTujuanPembelajaran
      tags: [tujuan-pembelajaran]
      summary: List Tujuan Pembelajaran for a subject + semester
      parameters:
        - name: subjectId
          in: query
          schema:
            type: string
        - name: calendarId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of Tujuan Pembelajaran
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TujuanPembelajaran"
    post:
      operationId: createTujuanPembelajaran
      tags: [tujuan-pembelajaran]
      summary: Manually add a Tujuan Pembelajaran entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TujuanPembelajaranInput"
      responses:
        "200":
          description: Created TP entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TujuanPembelajaran"
  /tp/import/analyze:
    post:
      operationId: analyzeTPImport
      tags: [tujuan-pembelajaran]
      summary: Analyze an uploaded file or pasted text with AI and map it to Tujuan Pembelajaran entries, regardless of its format
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TPImportAnalyzeInput"
      responses:
        "200":
          description: AI-mapped Tujuan Pembelajaran entries for verification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TPImportAnalyzeResult"
  /tp/bulk:
    post:
      operationId: bulkCreateTujuanPembelajaran
      tags: [tujuan-pembelajaran]
      summary: Create many Tujuan Pembelajaran entries at once
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkCreateTPInput"
      responses:
        "200":
          description: Number of TP entries created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkCreateTPResult"
  /tp/{id}:
    put:
      operationId: updateTujuanPembelajaran
      tags: [tujuan-pembelajaran]
      summary: Update a Tujuan Pembelajaran entry
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TujuanPembelajaranInput"
      responses:
        "200":
          description: Updated TP entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TujuanPembelajaran"
    delete:
      operationId: deleteTujuanPembelajaran
      tags: [tujuan-pembelajaran]
      summary: Delete a Tujuan Pembelajaran entry
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /tp/reorder:
    post:
      operationId: reorderTujuanPembelajaran
      tags: [tujuan-pembelajaran]
      summary: Reorder Tujuan Pembelajaran entries within a Lingkup Materi
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TPReorderInput"
      responses:
        "200":
          description: Reordered successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /jadwal:
    get:
      operationId: listJadwal
      tags: [jadwal]
      summary: List jadwal pelajaran milik guru yang login (atau semua jika admin)
      parameters:
        - name: teacherId
          in: query
          description: Filter by teacher id (admin only)
          schema:
            type: string
      responses:
        "200":
          description: List jadwal
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/JadwalEntry"
    post:
      operationId: createJadwal
      tags: [jadwal]
      summary: Buat jadwal baru
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JadwalInput"
      responses:
        "201":
          description: Jadwal dibuat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JadwalEntry"

  /jadwal/{id}:
    patch:
      operationId: updateJadwal
      tags: [jadwal]
      summary: Update jadwal
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JadwalInput"
      responses:
        "200":
          description: Jadwal diperbarui
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JadwalEntry"
    delete:
      operationId: deleteJadwal
      tags: [jadwal]
      summary: Hapus jadwal
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Berhasil dihapus
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /jadwal/import-preview:
    post:
      operationId: importJadwalPreview
      tags: [jadwal]
      summary: Ekstrak jadwal dari PDF dan preview pencocokan mata pelajaran (AI)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JadwalImportInput"
      responses:
        "200":
          description: Preview hasil ekstraksi jadwal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JadwalImportPreviewResponse"

  /jadwal/bulk:
    post:
      operationId: bulkCreateJadwal
      tags: [jadwal]
      summary: Simpan banyak jadwal sekaligus (admin sekolah)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JadwalBulkInput"
      responses:
        "200":
          description: Jadwal berhasil disimpan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JadwalBulkResult"

  /rekap/absensi:
    get:
      operationId: getRekapAbsensi
      tags: [rekap]
      summary: Rekap absensi bulanan per kelas (6 bulan terakhir)
      parameters:
        - name: kelas
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Data rekap absensi
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RekapAbsensiResponse"

  /rekap/nilai:
    get:
      operationId: getRekapNilai
      tags: [rekap]
      summary: Rekap nilai per mata pelajaran/kelas
      parameters:
        - name: kelas
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Data rekap nilai
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RekapNilaiResponse"

  /journal:
    get:
      operationId: listJournalEntries
      tags: [journal]
      summary: List teaching journal entries
      parameters:
        - name: subjectId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of journal entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/JournalEntry"
    post:
      operationId: createJournalEntry
      tags: [journal]
      summary: Create a teaching journal entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryInput"
      responses:
        "200":
          description: Created journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
  /journal/{id}:
    patch:
      operationId: updateJournalEntry
      tags: [journal]
      summary: Update a journal entry
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryInput"
      responses:
        "200":
          description: Updated journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
    delete:
      operationId: deleteJournalEntry
      tags: [journal]
      summary: Delete a journal entry
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /attendance:
    get:
      operationId: listAttendance
      tags: [attendance]
      summary: List attendance records
      parameters:
        - name: subjectId
          in: query
          schema:
            type: string
        - name: date
          in: query
          schema:
            type: string
        - name: kelas
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of attendance records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AttendanceRecord"
    post:
      operationId: createAttendanceRecord
      tags: [attendance]
      summary: Create/record attendance for a student
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AttendanceRecordInput"
      responses:
        "200":
          description: Created attendance record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceRecord"

  /attendance/{id}:
    patch:
      operationId: updateAttendanceRecord
      tags: [attendance]
      summary: Update an attendance record's status
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AttendanceUpdate"
      responses:
        "200":
          description: Updated attendance record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceRecord"
    delete:
      operationId: deleteAttendanceRecord
      tags: [attendance]
      summary: Delete an attendance record
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /attendance/bulk:
    post:
      operationId: bulkCreateAttendance
      tags: [attendance]
      summary: Record the same attendance status for a group of students
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkCreateAttendanceInput"
      responses:
        "200":
          description: Bulk create result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkCreateResult"

  /attendance/rekap:
    get:
      operationId: getAttendanceRekap
      tags: [attendance]
      summary: Attendance recap grouped by date and class (school-wide — all teachers can see)
      responses:
        "200":
          description: Attendance recap
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceRekap"

  /attendance/bulk-kelas:
    delete:
      operationId: bulkDeleteAttendanceByKelas
      tags: [attendance]
      summary: Delete all attendance records for a specific class, date, and subject
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkDeleteAttendanceByKelasInput"
      responses:
        "200":
          description: Delete result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkCreateResult"

  /attendance/bulk-mixed:
    post:
      operationId: bulkMixedCreateAttendance
      tags: [attendance]
      summary: Record a different attendance status per student for one subject/date (daily input)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkMixedAttendanceInput"
      responses:
        "200":
          description: Bulk create result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkCreateResult"

  /grades:
    get:
      operationId: listGrades
      tags: [grades]
      summary: List grades
      parameters:
        - name: subjectId
          in: query
          schema:
            type: string
        - name: studentId
          in: query
          schema:
            type: string
        - name: calendarId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of grades
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      operationId: createGrade
      tags: [grades]
      summary: Record or update a grade (upsert by student/subject/calendar/jenis/lingkup materi/tp)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GradeInput"
      responses:
        "200":
          description: Created or updated grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"

  /grades/{id}:
    patch:
      operationId: updateGrade
      tags: [grades]
      summary: Update a grade's score
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GradeUpdate"
      responses:
        "200":
          description: Updated grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
    delete:
      operationId: deleteGrade
      tags: [grades]
      summary: Delete a grade
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /points:
    get:
      operationId: listPoints
      tags: [points]
      summary: List student points
      parameters:
        - name: studentId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of point records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PointRecord"
    post:
      operationId: createPoint
      tags: [points]
      summary: Record a point entry for a student
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PointRecordInput"
      responses:
        "200":
          description: Created point record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PointRecord"

  /points/{id}:
    patch:
      operationId: updatePoint
      tags: [points]
      summary: Update a point record
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PointUpdate"
      responses:
        "200":
          description: Updated point record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PointRecord"
    delete:
      operationId: deletePoint
      tags: [points]
      summary: Delete a point record
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /points/bulk:
    post:
      operationId: bulkCreatePoints
      tags: [points]
      summary: Record the same point entry for a group of students
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkCreatePointsInput"
      responses:
        "200":
          description: Bulk create result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkCreatePointsResult"

  /points/bulk-mixed:
    post:
      operationId: bulkMixedCreatePoints
      tags: [points]
      summary: Record a different (optional) point entry per student for one date (daily input)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkMixedPointsInput"
      responses:
        "200":
          description: Bulk create result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkCreatePointsResult"

  /academic-calendars:
    get:
      operationId: listAcademicCalendars
      tags: [academic-calendar]
      summary: List academic calendars for the current school
      responses:
        "200":
          description: List of academic calendars
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AcademicCalendar"
    post:
      operationId: createAcademicCalendar
      tags: [academic-calendar]
      summary: Create an academic calendar (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcademicCalendarInput"
      responses:
        "200":
          description: Created academic calendar
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcademicCalendar"
  /academic-calendars/{id}:
    delete:
      operationId: deleteAcademicCalendar
      tags: [academic-calendar]
      summary: Delete an academic calendar (admin only)
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /academic-weeks:
    get:
      operationId: listAcademicWeeks
      tags: [academic-calendar]
      summary: List weeks of a calendar
      parameters:
        - name: calendarId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of academic weeks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AcademicWeek"
    post:
      operationId: createAcademicWeek
      tags: [academic-calendar]
      summary: Create an academic week (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcademicWeekInput"
      responses:
        "200":
          description: Created academic week
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcademicWeek"
  /academic-weeks/{id}:
    put:
      operationId: updateAcademicWeek
      tags: [academic-calendar]
      summary: Update an academic week (admin only)
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcademicWeekInput"
      responses:
        "200":
          description: Updated academic week
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcademicWeek"
    delete:
      operationId: deleteAcademicWeek
      tags: [academic-calendar]
      summary: Delete an academic week (admin only)
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /prosem:
    get:
      operationId: listProsem
      tags: [prosem]
      summary: List the current teacher's prosem entries
      parameters:
        - name: calendarId
          in: query
          schema:
            type: string
        - name: subjectId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of prosem
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Prosem"
    post:
      operationId: createProsem
      tags: [prosem]
      summary: Create a prosem entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProsemInput"
      responses:
        "200":
          description: Created prosem
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Prosem"
  /prosem/{id}:
    delete:
      operationId: deleteProsem
      tags: [prosem]
      summary: Delete a prosem entry
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /prosem-items:
    get:
      operationId: listProsemItems
      tags: [prosem]
      summary: List items of a prosem
      parameters:
        - name: prosemId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of prosem items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProsemItem"
    post:
      operationId: createProsemItem
      tags: [prosem]
      summary: Create a prosem item
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProsemItemInput"
      responses:
        "200":
          description: Created prosem item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProsemItem"
  /prosem-items/{id}:
    put:
      operationId: updateProsemItem
      tags: [prosem]
      summary: Update a prosem item
      parameters:
        - $ref: "#/components/parameters/IdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProsemItemInput"
      responses:
        "200":
          description: Updated prosem item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProsemItem"
    delete:
      operationId: deleteProsemItem
      tags: [prosem]
      summary: Delete a prosem item
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /info-pekanan:
    get:
      operationId: getInfoPekanan
      tags: [info-pekanan]
      summary: Weekly info comparison (rencana from prosem vs realisasi from journal)
      parameters:
        - name: calendarId
          in: query
          schema:
            type: string
        - name: weekId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Weekly info payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfoPekanan"

  /modul-ajar:
    get:
      operationId: listModulAjar
      tags: [modul-ajar]
      summary: List generated modul ajar drafts for the current teacher
      parameters:
        - name: subjectId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of modul ajar drafts (metadata only)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ModulAjarSummary"

  /modul-ajar/generate:
    post:
      operationId: generateModulAjar
      tags: [modul-ajar]
      summary: Generate a new modul ajar draft with AI and save it
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateModulAjarInput"
      responses:
        "201":
          description: Generated modul ajar
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModulAjar"

  /modul-ajar/{id}:
    get:
      operationId: getModulAjar
      tags: [modul-ajar]
      summary: Get a saved modul ajar draft (with full content)
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Modul ajar draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModulAjar"
    delete:
      operationId: deleteModulAjar
      tags: [modul-ajar]
      summary: Delete a saved modul ajar draft
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /modul-ajar/{id}/docx:
    get:
      operationId: downloadModulAjarDocx
      tags: [modul-ajar]
      summary: Download a modul ajar draft as a .docx file
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: DOCX file
          content:
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary

  /soal-otomatis:
    get:
      operationId: listSoalOtomatis
      tags: [soal-otomatis]
      summary: List generated soal otomatis drafts for the current teacher
      parameters:
        - name: subjectId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of soal drafts (metadata only)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SoalOtomatisSummary"

  /soal-otomatis/generate:
    post:
      operationId: generateSoalOtomatis
      tags: [soal-otomatis]
      summary: Generate a new set of practice questions with AI and save it
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateSoalOtomatisInput"
      responses:
        "201":
          description: Generated soal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoalOtomatis"

  /soal-otomatis/{id}:
    get:
      operationId: getSoalOtomatis
      tags: [soal-otomatis]
      summary: Get a saved soal draft (with full content)
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Soal draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoalOtomatis"
    delete:
      operationId: deleteSoalOtomatis
      tags: [soal-otomatis]
      summary: Delete a saved soal draft
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /soal-otomatis/{id}/docx:
    get:
      operationId: downloadSoalOtomatisDocx
      tags: [soal-otomatis]
      summary: Download a soal draft as a .docx file
      parameters:
        - $ref: "#/components/parameters/IdParam"
      responses:
        "200":
          description: DOCX file
          content:
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary

components:
  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
      required:
        - success

    JadwalEntry:
      type: object
      properties:
        id:
          type: string
        teacherId:
          type: string
        teacherName:
          type: string
        subjectId:
          type: string
        subjectName:
          type: string
        kelas:
          type: string
        hari:
          type: string
          enum: [Senin, Selasa, Rabu, Kamis, Jumat, Sabtu]
        jamMulai:
          type: string
        jamSelesai:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, teacherId, teacherName, subjectId, subjectName, kelas, hari, jamMulai, jamSelesai, createdAt]

    JadwalImportPreviewItem:
      type: object
      properties:
        kelas:
          type: string
        hari:
          type: string
        jamMulai:
          type: string
        jamSelesai:
          type: string
        mapelRaw:
          type: string
          description: Nama mata pelajaran mentah dari PDF
        subjectId:
          type: string
          nullable: true
        subjectName:
          type: string
          nullable: true
        teacherName:
          type: string
          nullable: true
        matched:
          type: boolean
      required: [kelas, hari, jamMulai, jamSelesai, mapelRaw, matched]

    JadwalImportInput:
      type: object
      properties:
        fileBase64:
          type: string
          description: Base64-encoded PDF content
      required: [fileBase64]

    JadwalImportPreviewResponse:
      type: object
      properties:
        preview:
          type: array
          items:
            $ref: "#/components/schemas/JadwalImportPreviewItem"
      required: [preview]

    JadwalBulkInput:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/JadwalBulkEntry"
      required: [entries]

    JadwalBulkEntry:
      type: object
      properties:
        subjectId:
          type: string
        kelas:
          type: string
        hari:
          type: string
        jamMulai:
          type: string
        jamSelesai:
          type: string
      required: [subjectId, kelas, hari, jamMulai, jamSelesai]

    JadwalBulkResult:
      type: object
      properties:
        inserted:
          type: integer
      required: [inserted]

    JadwalInput:
      type: object
      properties:
        subjectId:
          type: string
        kelas:
          type: string
        hari:
          type: string
          enum: [Senin, Selasa, Rabu, Kamis, Jumat, Sabtu]
        jamMulai:
          type: string
        jamSelesai:
          type: string
      required: [subjectId, kelas, hari, jamMulai, jamSelesai]

    RekapAbsensiItem:
      type: object
      properties:
        bulan:
          type: string
        kelas:
          type: string
        hadir:
          type: integer
        izin:
          type: integer
        sakit:
          type: integer
        alpa:
          type: integer
        total:
          type: integer
      required: [bulan, kelas, hadir, izin, sakit, alpa, total]

    RekapAbsensiResponse:
      type: object
      properties:
        kelasOptions:
          type: array
          items:
            type: string
        data:
          type: array
          items:
            $ref: "#/components/schemas/RekapAbsensiItem"
      required: [kelasOptions, data]

    RekapNilaiDistribusiItem:
      type: object
      properties:
        range:
          type: string
        jumlah:
          type: integer
      required: [range, jumlah]

    RekapNilaiSubject:
      type: object
      properties:
        subjectId:
          type: string
        subjectName:
          type: string
        kelas:
          type: string
        rataRata:
          type: number
          nullable: true
        nilaiMin:
          type: number
          nullable: true
        nilaiMax:
          type: number
          nullable: true
        jumlahNilai:
          type: integer
        distribusi:
          type: array
          items:
            $ref: "#/components/schemas/RekapNilaiDistribusiItem"
      required: [subjectId, subjectName, kelas, jumlahNilai, distribusi]

    RekapNilaiResponse:
      type: object
      properties:
        kelasOptions:
          type: array
          items:
            type: string
        subjects:
          type: array
          items:
            $ref: "#/components/schemas/RekapNilaiSubject"
      required: [kelasOptions, subjects]

    TeacherProgress:
      type: object
      properties:
        teacherId:
          type: string
        jurnalBulanIni:
          type: integer
        dokumenTotal:
          type: integer
        dokumenSelesai:
          type: integer
        kelengkapanPersen:
          type: number
      required: [teacherId, jurnalBulanIni, dokumenTotal, dokumenSelesai, kelengkapanPersen]

    Teacher:
      type: object
      properties:
        id:
          type: string
        username:
          type: string
        name:
          type: string
        jabatan:
          type: array
          items:
            type: string
            enum: [kepala_sekolah, wakasek, guru, wali_kelas]
        mapel:
          type: array
          items:
            type: string
          nullable: true
        wakasekBidang:
          type: string
          nullable: true
        waliKelasKelas:
          type: string
          nullable: true
        kelasDiampu:
          type: array
          items:
            type: string
        school:
          type: string
          nullable: true
        photoUrl:
          type: string
          nullable: true
        bio:
          type: string
          nullable: true
        sebutan:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        isAdmin:
          type: boolean
      required: [id, username, name, jabatan, kelasDiampu, createdAt, isAdmin]

    RegisterInput:
      type: object
      properties:
        name:
          type: string
        jabatan:
          type: array
          items:
            type: string
            enum: [kepala_sekolah, wakasek, guru, wali_kelas]
          minItems: 1
        mapel:
          type: array
          items:
            type: string
        wakasekBidang:
          type: string
          enum: [Kurikulum, Kesiswaan]
        waliKelasKelas:
          type: string
        kelasDiampu:
          type: array
          items:
            type: string
        school:
          type: string
        username:
          type: string
        password:
          type: string
      required: [name, jabatan, kelasDiampu, school, username, password]

    LoginInput:
      type: object
      properties:
        username:
          type: string
        password:
          type: string
      required: [username, password]

    UpdateTeacherInput:
      type: object
      properties:
        name:
          type: string
        jabatan:
          type: array
          items:
            type: string
            enum: [kepala_sekolah, wakasek, guru, wali_kelas]
        mapel:
          type: array
          items:
            type: string
        wakasekBidang:
          type: string
        waliKelasKelas:
          type: string
        kelasDiampu:
          type: array
          items:
            type: string
        school:
          type: string
        bio:
          type: string
          maxLength: 500
        photoUrl:
          type: string
          maxLength: 400000
          pattern: "^$|^data:image/(jpeg|png|webp);base64,|^https?://"
        sebutan:
          type: string

    GenerateStudentAccountInput:
      type: object
      properties:
        regenerate:
          type: boolean

    StudentAccount:
      type: object
      properties:
        studentId:
          type: string
        username:
          type: string
        password:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [studentId, username, password, createdAt]

    Student:
      type: object
      properties:
        id:
          type: string
        nisn:
          type: string
          nullable: true
        namaLengkap:
          type: string
        kelas:
          type: string
        jenisKelamin:
          type: string
          enum: [L, P]
        school:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, nisn, namaLengkap, kelas, jenisKelamin, school, createdAt]

    StudentInput:
      type: object
      properties:
        nisn:
          type: string
          nullable: true
        namaLengkap:
          type: string
        kelas:
          type: string
        jenisKelamin:
          type: string
          enum: [L, P]
        school:
          type: string
      required: [namaLengkap, kelas, jenisKelamin, school]

    StudentImportAnalyzeInput:
      type: object
      properties:
        rows:
          type: array
          maxItems: 1000
          items:
            type: array
            items:
              type: string
      required: [rows]

    StudentImportAnalyzeResult:
      type: object
      properties:
        students:
          type: array
          items:
            $ref: "#/components/schemas/StudentInput"
      required: [students]

    TujuanPembelajaran:
      type: object
      properties:
        id:
          type: string
        subjectId:
          type: string
        calendarId:
          type: string
        lingkupMateri:
          type: integer
        tpNumber:
          type: integer
        description:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, subjectId, calendarId, lingkupMateri, tpNumber, description, createdAt]

    TujuanPembelajaranInput:
      type: object
      description: >
        tpNumber is not part of the input -- it is assigned automatically by the
        server as a continuous sequence across the whole subject+semester (not
        reset per Lingkup Materi).
      properties:
        subjectId:
          type: string
        calendarId:
          type: string
        lingkupMateri:
          type: integer
        description:
          type: string
      required: [subjectId, calendarId, lingkupMateri, description]

    TPReorderInput:
      type: object
      description: Reorder TP entries within a single Lingkup Materi by supplying their IDs in the desired order.
      properties:
        subjectId:
          type: string
        calendarId:
          type: string
        lingkupMateri:
          type: integer
        ids:
          type: array
          description: Ordered list of TP IDs for this Lingkup Materi
          items:
            type: string
          minItems: 1
      required: [subjectId, calendarId, lingkupMateri, ids]

    TPImportAnalyzeInput:
      type: object
      description: >
        Provide either `rows` (pre-parsed spreadsheet rows) or `fileData` (base64 raw
        file bytes) with `fileName`/`fileType`. The AI recognizes the content regardless
        of layout or file format (spreadsheet, PDF, Word document, image, or plain text).
      properties:
        rows:
          type: array
          maxItems: 2000
          items:
            type: array
            items:
              type: string
        fileData:
          type: string
          description: Base64-encoded raw file content.
        fileName:
          type: string
        fileType:
          type: string
          description: MIME type of the uploaded file.
      required: []

    TPImportAnalyzeResult:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              lingkupMateri:
                type: integer
              tpNumber:
                type: integer
              description:
                type: string
            required: [lingkupMateri, tpNumber, description]
      required: [items]

    BulkCreateTPInput:
      type: object
      properties:
        subjectId:
          type: string
        calendarId:
          type: string
        items:
          type: array
          minItems: 1
          maxItems: 2000
          items:
            type: object
            properties:
              lingkupMateri:
                type: integer
              tpNumber:
                type: integer
              description:
                type: string
            required: [lingkupMateri, tpNumber, description]
      required: [subjectId, calendarId, items]

    BulkCreateTPResult:
      type: object
      properties:
        count:
          type: integer
        skipped:
          type: integer
      required: [count, skipped]

    BulkCreateStudentsInput:
      type: object
      properties:
        students:
          type: array
          minItems: 1
          maxItems: 1000
          items:
            $ref: "#/components/schemas/StudentInput"
      required: [students]

    BulkCreatePointsInput:
      type: object
      properties:
        studentIds:
          type: array
          minItems: 1
          maxItems: 500
          items:
            type: string
        jenis:
          type: string
          enum: [positif, negatif]
        poin:
          type: number
        keterangan:
          type: string
        tanggal:
          type: string
      required: [studentIds, jenis, poin, keterangan, tanggal]

    BulkCreatePointsResult:
      type: object
      properties:
        count:
          type: integer
      required: [count]

    BulkCreateResult:
      type: object
      properties:
        count:
          type: integer
      required: [count]

    BulkCreateAttendanceInput:
      type: object
      properties:
        studentIds:
          type: array
          minItems: 1
          maxItems: 500
          items:
            type: string
        tanggal:
          type: string
        status:
          type: string
          enum: [hadir, izin, sakit, alpa]
      required: [studentIds, tanggal, status]

    BulkMixedAttendanceEntry:
      type: object
      properties:
        studentId:
          type: string
        status:
          type: string
          enum: [hadir, izin, sakit, alpa]
      required: [studentId, status]

    BulkMixedAttendanceInput:
      type: object
      properties:
        tanggal:
          type: string
        entries:
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: "#/components/schemas/BulkMixedAttendanceEntry"
      required: [tanggal, entries]

    BulkMixedPointsEntry:
      type: object
      properties:
        studentId:
          type: string
        jenis:
          type: string
          enum: [positif, negatif]
        poin:
          type: number
        keterangan:
          type: string
      required: [studentId, jenis, poin, keterangan]

    BulkMixedPointsInput:
      type: object
      properties:
        tanggal:
          type: string
        entries:
          type: array
          minItems: 0
          maxItems: 500
          items:
            $ref: "#/components/schemas/BulkMixedPointsEntry"
      required: [tanggal, entries]

    BulkCreateStudentsResult:
      type: object
      properties:
        count:
          type: integer
        skipped:
          type: integer
      required: [count, skipped]

    Subject:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        teacherId:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, name, teacherId, createdAt]

    SubjectInput:
      type: object
      properties:
        name:
          type: string
        teacherId:
          type: string
      required: [name, teacherId]

    AdminDocument:
      type: object
      properties:
        id:
          type: string
        subjectId:
          type: string
        name:
          type: string
        description:
          type: string
        fileName:
          type: string
          description: Original file name as uploaded.
        fileType:
          type: string
          description: MIME type of the uploaded file.
        fileSize:
          type: integer
          description: File size in bytes.
        uploadedAt:
          type: string
          format: date-time
      required: [id, subjectId, name, uploadedAt]

    AdminDocumentInput:
      type: object
      properties:
        subjectId:
          type: string
        name:
          type: string
        description:
          type: string
        fileData:
          type: string
          description: Base64-encoded file content, stored directly in the database.
        fileName:
          type: string
        fileType:
          type: string
        fileSize:
          type: integer
      required: [subjectId, name, fileData, fileName]

    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: string

    ModulAjarSummary:
      type: object
      properties:
        id:
          type: string
        subjectId:
          type: string
        materi:
          type: string
        alokasiWaktu:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, subjectId, materi, alokasiWaktu, createdAt]

    ModulAjar:
      type: object
      properties:
        id:
          type: string
        subjectId:
          type: string
        materi:
          type: string
        alokasiWaktu:
          type: string
        content:
          type: object
          description: Full structured Kurikulum Merdeka modul ajar draft generated by AI.
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
      required: [id, subjectId, materi, alokasiWaktu, content, createdAt]

    GenerateModulAjarInput:
      type: object
      properties:
        subjectId:
          type: string
        materi:
          type: string
        alokasiWaktu:
          type: string
        kelas:
          type: string
      required: [subjectId, materi, alokasiWaktu]

    SoalOtomatisSummary:
      type: object
      properties:
        id:
          type: string
        subjectId:
          type: string
        materi:
          type: string
        jumlahSoal:
          type: integer
        jenisSoal:
          type: string
          enum: [pilihan_ganda, esai]
        tingkatKesulitan:
          type: string
          enum: [mudah, sedang, sulit]
        createdAt:
          type: string
          format: date-time
      required: [id, subjectId, materi, jumlahSoal, jenisSoal, tingkatKesulitan, createdAt]

    SoalOtomatis:
      type: object
      properties:
        id:
          type: string
        subjectId:
          type: string
        materi:
          type: string
        jumlahSoal:
          type: integer
        jenisSoal:
          type: string
          enum: [pilihan_ganda, esai]
        tingkatKesulitan:
          type: string
          enum: [mudah, sedang, sulit]
        content:
          type: object
          description: Full structured soal draft (questions + answer key) generated by AI.
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
      required:
        [id, subjectId, materi, jumlahSoal, jenisSoal, tingkatKesulitan, content, createdAt]

    GenerateSoalOtomatisInput:
      type: object
      properties:
        subjectId:
          type: string
        materi:
          type: string
        jumlahSoal:
          type: integer
          minimum: 1
          maximum: 50
        jenisSoal:
          type: string
          enum: [pilihan_ganda, esai]
        tingkatKesulitan:
          type: string
          enum: [mudah, sedang, sulit]
      required: [subjectId, materi, jumlahSoal, jenisSoal, tingkatKesulitan]

    JournalEntry:
      type: object
      properties:
        id:
          type: string
        subjectId:
          type: string
        teacherId:
          type: string
        tanggal:
          type: string
        kelas:
          type: string
        materi:
          type: string
        catatan:
          type: string
        prosemItemId:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, subjectId, teacherId, tanggal, kelas, materi, createdAt]

    JournalEntryInput:
      type: object
      properties:
        subjectId:
          type: string
        tanggal:
          type: string
        kelas:
          type: string
        materi:
          type: string
        catatan:
          type: string
        prosemItemId:
          type: string
          nullable: true
      required: [subjectId, tanggal, kelas, materi]

    AttendanceRecord:
      type: object
      properties:
        id:
          type: string
        studentId:
          type: string
        tanggal:
          type: string
        status:
          type: string
          enum: [hadir, izin, sakit, alpa]
        filledByTeacherId:
          type: ["string", "null"]
        filledByTeacherName:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
      required: [id, studentId, tanggal, status, createdAt]

    AttendanceRecordInput:
      type: object
      properties:
        studentId:
          type: string
        tanggal:
          type: string
        status:
          type: string
          enum: [hadir, izin, sakit, alpa]
      required: [studentId, tanggal, status]

    AttendanceUpdate:
      type: object
      properties:
        status:
          type: string
          enum: [hadir, izin, sakit, alpa]
      required: [status]

    Grade:
      type: object
      properties:
        id:
          type: string
        studentId:
          type: string
        subjectId:
          type: string
        calendarId:
          type: string
        jenis:
          type: string
          enum: [formatif, sumatif_lm, sumatif_tengah, sumatif_akhir]
        lingkupMateri:
          type: ["integer", "null"]
          minimum: 1
        tpNumber:
          type: ["integer", "null"]
          minimum: 1
        nilai:
          type: number
        createdAt:
          type: string
          format: date-time
      required: [id, studentId, subjectId, calendarId, jenis, nilai, createdAt]

    GradeInput:
      type: object
      properties:
        studentId:
          type: string
        subjectId:
          type: string
        calendarId:
          type: string
        jenis:
          type: string
          enum: [formatif, sumatif_lm, sumatif_tengah, sumatif_akhir]
        lingkupMateri:
          type: integer
          minimum: 1
        tpNumber:
          type: integer
          minimum: 1
        nilai:
          type: number
      required: [studentId, subjectId, calendarId, jenis, nilai]

    GradeUpdate:
      type: object
      properties:
        nilai:
          type: number
      required: [nilai]

    PointRecord:
      type: object
      properties:
        id:
          type: string
        studentId:
          type: string
        jenis:
          type: string
          enum: [positif, negatif]
        poin:
          type: number
        keterangan:
          type: string
        tanggal:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, studentId, jenis, poin, keterangan, tanggal, createdAt]

    PointRecordInput:
      type: object
      properties:
        studentId:
          type: string
        jenis:
          type: string
          enum: [positif, negatif]
        poin:
          type: number
        keterangan:
          type: string
        tanggal:
          type: string
      required: [studentId, jenis, poin, keterangan, tanggal]

    PointUpdate:
      type: object
      properties:
        jenis:
          type: string
          enum: [positif, negatif]
        poin:
          type: number
        keterangan:
          type: string
        tanggal:
          type: string
      required: [jenis, poin, keterangan, tanggal]

    DashboardSummary:
      type: object
      properties:
        totalSiswa:
          type: integer
        totalGuru:
          type: integer
        totalDokumen:
          type: integer
        jurnalHariIniTerisi:
          type: boolean
        progresJurnalBulanIni:
          type: array
          items:
            type: object
            properties:
              minggu:
                type: string
              jumlah:
                type: integer
            required: [minggu, jumlah]
        kelengkapanAdministrasiPersen:
          type: number
        schoolName:
          type: string
        tahunAjaran:
          type: string
        semester:
          type: string
      required:
        - totalSiswa
        - totalGuru
        - totalDokumen
        - jurnalHariIniTerisi
        - progresJurnalBulanIni
        - kelengkapanAdministrasiPersen
        - schoolName
        - tahunAjaran
        - semester

    KepsekTeacherProgress:
      type: object
      properties:
        username:
          type: string
        name:
          type: string
        jabatan:
          type: array
          items:
            type: string
        mapel:
          type: array
          items:
            type: string
          nullable: true
        jurnalBulanIni:
          type: integer
        dokumenTotal:
          type: integer
        dokumenSelesai:
          type: integer
        kelengkapanPersen:
          type: number
      required: [username, name, jabatan, jurnalBulanIni, dokumenTotal, dokumenSelesai, kelengkapanPersen]

    KepsekOverview:
      type: object
      properties:
        teachers:
          type: array
          items:
            $ref: "#/components/schemas/KepsekTeacherProgress"
      required: [teachers]

    KurikulumTeacherDocs:
      type: object
      properties:
        username:
          type: string
        name:
          type: string
        mapel:
          type: array
          items:
            type: string
          nullable: true
        subjects:
          type: array
          items:
            type: object
            properties:
              subjectId:
                type: string
              subjectName:
                type: string
              documents:
                type: array
                items:
                  $ref: "#/components/schemas/AdminDocument"
            required: [subjectId, subjectName, documents]
      required: [username, name, subjects]

    KurikulumOverview:
      type: object
      properties:
        teachers:
          type: array
          items:
            $ref: "#/components/schemas/KurikulumTeacherDocs"
      required: [teachers]

    KesiswaanKelasRekap:
      type: object
      properties:
        kelas:
          type: string
        totalSiswa:
          type: integer
        hadir:
          type: integer
        izin:
          type: integer
        sakit:
          type: integer
        alpa:
          type: integer
        totalPoinPositif:
          type: integer
        totalPoinNegatif:
          type: integer
      required: [kelas, totalSiswa, hadir, izin, sakit, alpa, totalPoinPositif, totalPoinNegatif]

    KesiswaanSiswaPoin:
      type: object
      properties:
        studentId:
          type: string
        namaLengkap:
          type: string
        kelas:
          type: string
        totalPoin:
          type: integer
      required: [studentId, namaLengkap, kelas, totalPoin]

    KesiswaanSiswaAbsensi:
      type: object
      properties:
        studentId:
          type: string
        namaLengkap:
          type: string
        kelas:
          type: string
        hadir:
          type: integer
        izin:
          type: integer
        sakit:
          type: integer
        alpa:
          type: integer
        totalSesi:
          type: integer
        pctHadir:
          type: number
        totalPoinPositif:
          type: integer
        totalPoinNegatif:
          type: integer
      required: [studentId, namaLengkap, kelas, hadir, izin, sakit, alpa, totalSesi, pctHadir, totalPoinPositif, totalPoinNegatif]

    KesiswaanOverview:
      type: object
      properties:
        perKelas:
          type: array
          items:
            $ref: "#/components/schemas/KesiswaanKelasRekap"
        siswaPoinTerbanyak:
          type: array
          items:
            $ref: "#/components/schemas/KesiswaanSiswaPoin"
        siswaPoinPositifTerbanyak:
          type: array
          items:
            $ref: "#/components/schemas/KesiswaanSiswaPoin"
      required: [perKelas, siswaPoinTerbanyak, siswaPoinPositifTerbanyak]

    WaliKelasSiswaRekap:
      type: object
      properties:
        studentId:
          type: string
        nisn:
          type: string
          nullable: true
        namaLengkap:
          type: string
        jenisKelamin:
          type: string
        hadir:
          type: integer
        izin:
          type: integer
        sakit:
          type: integer
        alpa:
          type: integer
        rataNilai:
          type: number
          nullable: true
        totalPoin:
          type: integer
      required: [studentId, nisn, namaLengkap, jenisKelamin, hadir, izin, sakit, alpa, totalPoin]

    WaliKelasRekap:
      type: object
      properties:
        kelas:
          type: string
        siswa:
          type: array
          items:
            $ref: "#/components/schemas/WaliKelasSiswaRekap"
      required: [kelas, siswa]

    AcademicCalendar:
      type: object
      properties:
        id:
          type: string
        school:
          type: string
        createdBy:
          type: string
        tahunAjaran:
          type: string
        semester:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, school, createdBy, tahunAjaran, semester, createdAt]

    AcademicCalendarInput:
      type: object
      properties:
        tahunAjaran:
          type: string
          minLength: 1
        semester:
          type: string
          minLength: 1
      required: [tahunAjaran, semester]

    AcademicWeek:
      type: object
      properties:
        id:
          type: string
        calendarId:
          type: string
        pekanKe:
          type: integer
        tanggalMulai:
          type: string
        tanggalSelesai:
          type: string
        jenis:
          type: string
        keterangan:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, calendarId, pekanKe, tanggalMulai, tanggalSelesai, jenis, createdAt]

    AcademicWeekInput:
      type: object
      properties:
        calendarId:
          type: string
          minLength: 1
        pekanKe:
          type: integer
        tanggalMulai:
          type: string
          minLength: 1
        tanggalSelesai:
          type: string
          minLength: 1
        jenis:
          type: string
          minLength: 1
        keterangan:
          type: string
          nullable: true
      required: [calendarId, pekanKe, tanggalMulai, tanggalSelesai, jenis]

    Prosem:
      type: object
      properties:
        id:
          type: string
        teacherId:
          type: string
        subjectId:
          type: string
        calendarId:
          type: string
        kelas:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, teacherId, subjectId, calendarId, kelas, createdAt]

    ProsemInput:
      type: object
      properties:
        subjectId:
          type: string
          minLength: 1
        calendarId:
          type: string
          minLength: 1
        kelas:
          type: string
          minLength: 1
      required: [subjectId, calendarId, kelas]

    ProsemItem:
      type: object
      properties:
        id:
          type: string
        prosemId:
          type: string
        weekId:
          type: string
        kd:
          type: string
          nullable: true
        materi:
          type: string
        jp:
          type: integer
          nullable: true
        catatan:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, prosemId, weekId, materi, createdAt]

    ProsemItemInput:
      type: object
      properties:
        prosemId:
          type: string
          minLength: 1
        weekId:
          type: string
          minLength: 1
        kd:
          type: string
          nullable: true
        materi:
          type: string
          minLength: 1
        jp:
          type: integer
          nullable: true
        catatan:
          type: string
          nullable: true
      required: [prosemId, weekId, materi]

    InfoPekananItem:
      type: object
      properties:
        prosemItemId:
          type: string
          nullable: true
        subjectId:
          type: string
        subjectName:
          type: string
        kelas:
          type: string
        kd:
          type: string
          nullable: true
        materi:
          type: string
        jp:
          type: integer
          nullable: true
        status:
          type: string
        journalEntryId:
          type: string
          nullable: true
      required: [subjectId, subjectName, kelas, materi, status]

    InfoPekanan:
      type: object
      properties:
        weekId:
          type: string
          nullable: true
        pekanKe:
          type: integer
          nullable: true
        tanggalMulai:
          type: string
          nullable: true
        tanggalSelesai:
          type: string
          nullable: true
        jenis:
          type: string
          nullable: true
        totalRencana:
          type: integer
        totalSesuai:
          type: integer
        totalTertinggal:
          type: integer
        totalDiDepan:
          type: integer
        items:
          type: array
          items:
            $ref: "#/components/schemas/InfoPekananItem"
      required: [totalRencana, totalSesuai, totalTertinggal, totalDiDepan, items]

    AttendanceRekapGroup:
      type: object
      properties:
        tanggal:
          type: string
        kelas:
          type: string
        hadir:
          type: integer
        izin:
          type: integer
        sakit:
          type: integer
        alpa:
          type: integer
        total:
          type: integer
        filledByTeacherName:
          type: ["string", "null"]
      required: [tanggal, kelas, hadir, izin, sakit, alpa, total]

    AttendanceRekap:
      type: object
      properties:
        groups:
          type: array
          items:
            $ref: "#/components/schemas/AttendanceRekapGroup"
      required: [groups]

    BulkDeleteAttendanceByKelasInput:
      type: object
      properties:
        kelas:
          type: string
        tanggal:
          type: string
      required: [kelas, tanggal]

    RoleJurnalEntry:
      type: object
      properties:
        id:
          type: string
        teacherName:
          type: string
        subjectName:
          type: string
        kelas:
          type: string
        tanggal:
          type: string
        materi:
          type: string
        catatan:
          type: string
          nullable: true
      required: [id, teacherName, subjectName, kelas, tanggal, materi]

    RoleJurnalResponse:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/RoleJurnalEntry"
      required: [entries]

