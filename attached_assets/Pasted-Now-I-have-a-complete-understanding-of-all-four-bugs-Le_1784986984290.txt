Now I have a complete understanding of all four bugs. Let me plan the fixes:

Bug 1 & 2: Empty exp/koin/lencana and no hafalan on public profile

The PublicProfileScreen already has the UI for showing stats (profile.level, profile.coins, profile.exp) and they display '—' when null. The issue is that the profile API doesn't return these fields.

Fix: Update server/komunikasi.js GET /profile/:role/:id to:

For siswa: include level, exp, coins, stiker_layout from students table
For both roles: return the badges array by joining with student_badges and badges tables
Use badge IDs starting with hafalan_kali_ and hafalan_bagi_ to represent hafalan achievements in the public profile
I also need to update PublicProfileScreen to render the badges and hafalan section.

Bug 3: Guru can't visit full student profile

GuruDashboardScreen doesn't listen for the tomat:visit-profile event that fires when a guru clicks "Lihat Profil" in the modal. I need to add a useEffect hook to handle this navigation event.

Setting up state to track the visited profile and rendering PublicProfileScreen as an overlay when a profile is selected, with a back button that clears the state.

Bug 4: Notification unread badge persists after clicking notification

When a guru is already on the komunikasi tab and receives a new message notification, clicking it doesn't navigate to the correct target because CommunicationScreen has already applied its initial target. I need to fix the initialTargetAppliedRef logic in CommunicationScreen to handle this case.

I could use a key prop that forces remounting when the target changes, which would reset the component and re-fetch contacts. Or I could track the last applied target reference instead of just a boolean, so it re-applies when initialTarget changes.

Updating the ref to store the actual target identity...

There's also the notification bell in PlayerHeader that triggers communication, but that's a separate guru-specific issue. Let me outline the complete fix strategy: first, I need to update the server API to include stats and badges in the profile response by joining with the student badges table.

Second, I'll modify PublicProfileScreen to display hafalan badges below the stats section—the full profile screen is what matters here, not the quick modal preview.

Third, I need to add an event listener in GuruDashboardScreen to handle the tomat:visit-profile event and render the profile screen when a profile is visited.

Fourth, I'll fix the CommunicationScreen by tracking the last applied target instead of just a boolean flag.

Now I'm ready to start implementing these fixes.

For the server API query, I need to fetch level, exp, coins, and stiker_layout from the siswa table, plus join the student_badges and badges tables to retrieve the list of earned badges with their details like id, name, icon, and color.

For the guru profile query, since teachers don't have these gamification fields, I'm returning null values for equipped_bingkai, equipped_spanduk, equipped_pet_skin, level, exp, coins, and badges, while still pulling their kelas_diampu as the kelas field.

Now I'm moving on to display the hafalan badges in the PublicProfileScreen, grouping them by type like perkalian and pembagian, and then I need to handle the GuruDashboardScreen updates.

For the GuruDashboardScreen, I'm setting up a listener for the profile visit event that captures the visited profile data and renders the PublicProfileScreen modal when triggered, with a way to close it.

For the CommunicationScreen fixes, I'm adding a ref to track the last applied initialTarget reference to prevent unnecessary re-renders, then checking if the current initialTarget matches what was already applied before proceeding with contact loading.

Looking at the screenshot more carefully, it's showing the full PublicProfileScreen with the back button and header, not just a modal preview—so that's the component that needs the stat cards populated.

The issue is that the server API isn't returning the stats data, which is why they appear empty. Once I fix the API to include those stats, they'll automatically display in the screen. For the modal version, I could add stats there too, but the user's report is specifically about the full screen, so I'll focus on that.

I'm also checking how the notification metadata flows through the system—when a private message notification comes in, it includes the conversation type, sender ID, and sender role, and then in the CommunicationScreen I'm using that metadata to find the matching contact and set up the conversation properly.

The real bug I'm tracking down is that when you're already on the komunikasi tab and a new notification arrives, clicking it doesn't auto-select the contact or mark messages as read because the effect that handles the initial target isn't re-running. The fix is to track the actual identity of the last applied target instead of just a boolean flag, so the effect knows when something has actually changed.

Now I'm implementing all these fixes together, and I need to be careful with the SQL query structure when adding the new fields to the server profile endpoint.

Writing the server endpoint... Completing the query logic... Finishing the response object with the remaining profile fields, then wrapping up the endpoint. Now I'm thinking about the PublicProfileScreen component—I need to add a section that displays hafalan badges for multiplication and division mastery, filtering them by their ID prefixes like hafalan_kali_ and hafalan_bagi_ and rendering them as colored pills. Now I'm ready to implement the fixes. I need to update the profile endpoint to include badges and hafalan achievements, modify the public profile screen to display these stats and achievements, add stats to the modal for consistency, set up the profile visit listener in the guru dashboard, and fix the communication screen reference issue.

Writing code changes... Querying student profile data... Writing component JSX... Writing component styles...

Now I'm thinking about how to add a stats row to the PublicProfileModal, keeping it compact with just three columns between the bio section and action buttons.

For the GuruDashboardScreen, I need to import the PublicProfileScreen component and set up event listening to handle profile visits. When a profile visit event fires, I'll display the PublicProfileScreen and provide a way to navigate back by clearing the visited profile state.

For the CommunicationScreen, I'm updating a reference variable to track the initial target that was applied.

Now I'm noticing a potential issue with the effect dependency array — initialTarget is included in the dependencies, which means the effect will re-run whenever initialTarget changes. The reference comparison initialTargetAppliedRef.current === initialTarget checks object identity, so if a new object is passed each time even with the same content, it will trigger re-application as intended. Since initialTarget is already in the dependency array, the effect will properly respond to changes. Now the effect will re-apply whenever initialTarget changes to a different object, since setKomunikasiTarget creates a new object each time a notification is clicked. I need to move the ref assignment to after the if/else processing block so it only gets set once the contact lookup and selection logic completes.

Setting up the effect to handle both private and forum conversation types... There's another issue with the notification badge. When a pesan notification is clicked, it triggers the communication handler which sets komunikasiTarget, but I need to trace through how that flows to make sure the auto-selection logic picks it up correctly.

The problem is that notification.metadata gets freshly parsed from the database each time, so it's always a new object reference. This means when I check initialTargetAppliedRef.current === initialTarget, it should correctly detect that the target has changed. But when the contacts load and the effect re-runs with the same initialTarget, I need to verify the comparison still works as expected. So when contacts reload, the effect checks if the ref already matches the new target and skips if it does—that prevents duplicate processing. And if a new notification arrives while already on the komunikasi tab, the different object reference triggers the effect to apply the new target correctly. This approach handles both the initial load and subsequent notification clicks.

Now I'm ready to implement these changes, starting with updating GuruDashboardScreen to import the profile screen, add state for tracking visited profiles, and set up the effect to handle navigation.

Now I have the full picture of all four bugs. Let me implement all fixes in parallel:

Server API: Extend profile endpoint with level/exp/coins/stikerLayout/badges
PublicProfileScreen: Add hafalan badges section
PublicProfileModal: Add stat cards (level/koin/exp)
GuruDashboardScreen: Handle tomat:visit-profile event
CommunicationScreen: Fix initialTargetAppliedRef so re-opening a chat clears the unread badge